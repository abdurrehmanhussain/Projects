{
  "name": "api-grpc-playwright",
  "version": "1.0.0",
  "description": "Playwright project with gRPC CRUD operations using grpcb.in",
  "scripts": {
    "test": "npx playwright test",
    "test:grpc": "npx playwright test --grep @grpc",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.9.0",
    "@grpc/proto-loader": "^0.7.10"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
